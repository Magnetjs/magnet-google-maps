{
  "name": "magnet-google-maps",
  "version": "0.1.0",
  "description": "Readme",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "babel src --out-dir dist",
    "start": "babel src --out-dir dist",
    "test": "babel-node tests/index.js | tap-spec",
    "watch": "nodemon --ignore dist --exec \"babel src --out-dir dist --source-maps\"",
    "watch-test": "nodemon --delay 0.5 --ignore src --exec babel-node tests/index.js | tap-spec",
    "watch-all": "concurrently \"npm run watch\" \"npm run watch-test\""
  },
  "keywords": [
    "magnet",
    "google-maps"
  ],
  "author": "Kieve Chua <kievechua@gmail.com> (https://github.com/kievechua)",
  "license": "MIT",
  "files": [
    "README",
    "CHANGELOG.md",
    "dist"
  ],
  "dependencies": {
    "@google/maps": "^0.3.1",
    "magnet-core": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.2",
    "babel-plugin-transform-async-to-generator": "^6.7.0",
    "babel-plugin-transform-runtime": "^6.6.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "concurrently": "^2.0.0",
    "nodemon": "^1.9.1",
    "standard": "^8.6.0"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/Magnetjs/magnet-google-map.git"
  },
  "bugs": {
    "url": "https://github.com/Magnetjs/magnet-google-map/issues"
  },
  "homepage": "https://github.com/Magnetjs/magnet-google-map#readme",
  "babel": {
    "presets": [
      "es2015",
      "stage-0"
    ],
    "plugins": [
      "transform-async-to-generator"
    ]
  }
}
